# Demo Project

This directory is the bundled fixture dataset for `specview demo` and `specview init --demo`.

It intentionally contains exactly 10 specifications:

- 4 `new`
- 3 `in_progress`
- 3 `done`

The files are both executable documentation for the first-run experience and deterministic fixtures for integration tests.
