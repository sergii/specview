---
specview:
  status: done
---

# Configuration contract

Define `.specview.yaml`, default `specs/`, project metadata, and loopback server settings.

## Acceptance

- `specview init` creates `.specview.yaml` and `specs/`.
- project name is optional.
- configuration remains repository-local and read-only from the dashboard.
